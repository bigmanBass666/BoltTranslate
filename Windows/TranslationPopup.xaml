<Window x:Class="TranslateSharp.Windows.TranslationPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="翻译结果" Height="Auto" Width="420"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Deactivated="OnDeactivated"
        KeyDown="OnKeyDown"
        MouseLeftButtonDown="OnMouseLeftButtonDown">
    <Border x:Name="PopupBorder" CornerRadius="12" Background="#F0FFFFFF" BorderBrush="#1A000000" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.25" Color="Black"/>
        </Border.Effect>
        <TextBox x:Name="TranslatedTextBox"
                 TextWrapping="Wrap"
                 MaxWidth="390"
                 MaxHeight="400"
                 IsReadOnly="True"
                 BorderThickness="0"
                 Background="Transparent"
                 Foreground="#1A1A1A"
                 FontSize="14"
                 FontFamily="Microsoft YaHei UI"
                 VerticalScrollBarVisibility="Auto"
                 Padding="16,14,16,14"
                 Cursor="Arrow"/>
    </Border>
</Window>
